# Groq, writing a cassette for every call so replay-mode CI can play them back.
provider: groq
mode: record
cache_enabled: true
cache_path: ${paths.cache_dir}/model_cache.sqlite
max_retries: 6
backoff_initial_s: 2.0
backoff_max_s: 60.0
timeout_s: 120.0
