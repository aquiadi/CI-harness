# Live calls, nothing written. Used by the nightly workflow.
mode: live
cache_enabled: true
cache_path: ${paths.cache_dir}/model_cache.sqlite
max_retries: 3
backoff_initial_s: 1.0
backoff_max_s: 30.0
timeout_s: 120.0
