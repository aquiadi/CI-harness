root: ${repo_root:}
data_dir: ${paths.root}/data
corpus_raw_dir: ${paths.data_dir}/corpus/raw
corpus_processed_dir: ${paths.data_dir}/corpus/processed
corpus_manifest_path: ${paths.data_dir}/corpus/manifest.json
index_dir: ${paths.data_dir}/index
eval_dir: ${paths.data_dir}/eval
runs_dir: ${paths.root}/runs
reports_dir: ${paths.root}/reports
cache_dir: ${paths.root}/.cache
prompts_dir: ${paths.root}/prompts
cassette_dir: ${paths.root}/tests/cassettes
baseline_path: ${paths.root}/baseline.json
